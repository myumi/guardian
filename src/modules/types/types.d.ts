export interface ColorInterface {
  colorName: string;
  colorCode: string;
  textColor: string;
  value: number;
}

export interface ColorWheelInterface {
  colorName: string;
  colorCode: string;
  textColor: string;
}